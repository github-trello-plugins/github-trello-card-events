machine:
  node:
    version: 7

deployment:
  master:
    branch: master
    commands:
      - git remote add dokku dokku@dokku.mpirikreview.com:github-trello-card-events
      - git push -f dokku master:master
